//Abre el poop up de subida de archivo en una ventana independiente
function abrirVentanaSubida(url) {
    window.open(url, '_blank', 'width=600,height=600,top=100,left=100');
}

// Cierra el popup
function closeWindow() {
    window.close();
}